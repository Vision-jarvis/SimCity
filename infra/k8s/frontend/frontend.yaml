# SimCity Next.js frontend, exposed via NodePort for the single-node k3s host.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels: { app: frontend }
  template:
    metadata:
      labels: { app: frontend }
    spec:
      containers:
        - name: frontend
          image: simcity-frontend  # tag set via kustomization images:
          ports:
            - containerPort: 3000
          env:
            - { name: NEXT_PUBLIC_API_URL, value: "http://api:8000" }
            - { name: NEXT_PUBLIC_WS_URL, value: "ws://api:8000/ws/stream" }
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector: { app: frontend }
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
